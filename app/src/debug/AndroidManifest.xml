<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application android:usesCleartextTraffic="true" tools:replace="android:usesCleartextTraffic">
        <!-- ADB-only hook: DUMP is a system/signature permission, unavailable to ordinary apps. -->
        <receiver android:name=".DebugNotificationReceiver" android:exported="true" android:permission="android.permission.DUMP" />
        <receiver android:name=".DebugReplyReceiver" android:exported="false" />
    </application>
</manifest>
